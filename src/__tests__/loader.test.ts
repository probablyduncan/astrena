import { describe, it } from 'vitest'

describe('astrena', () => {
  it.todo('arenaLoader loads channel blocks into the data store')
  it.todo('arenaLiveLoader.loadCollection returns channel blocks')
  it.todo('arenaLiveLoader.loadEntry returns a single block by id')
})
